# Meeting System

A simple event management system built with pure PHP following the MVC architecture.

## Features

- MVC (Model-View-Controller) architecture
- Built with plain PHP (no frameworks)
- Event and user management
- Clean and extendable code structure

## Installation

1. **Copy the project to your web server directory**

   Place the project folder inside your web server root (e.g., `htdocs` in XAMPP):

